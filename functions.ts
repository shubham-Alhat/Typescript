function addTwo(num: number): number {
  return num + 2;
  // return "hello world"
}

addTwo(6);

function loginUser(name: string, email: string) {
  return "Entered..";
}

loginUser("shubham", "shubham@email.com");

function getValue(num: number): boolean {
  return true;
}

export {};
