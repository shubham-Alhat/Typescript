const enum Seatchoices {
  AISLE = 10,
  MIDDLE,
  WINDOW,
}

// above are choices

let shubhamSeat = Seatchoices.AISLE;

// only three choices so code became so robust and secured

// by default they have there number
// when we hover ---->  (enum member) Seatchoices.AISLE = 0
